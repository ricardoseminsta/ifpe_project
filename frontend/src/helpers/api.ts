import axios from "axios";
import { VisitsType } from './visits';

const baseURL = "https://sheet.best/api/sheets/817ff436-dbaa-4ebb-999f-efe65d82ad87"

export const api = {
    setVisit: async () => {
        
        let response = await axios.post(baseURL, )
    }
}